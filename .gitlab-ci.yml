image: node:12.18.3

stages:
  - test

cache:
  paths:
    - node_modules/

tests:
  stage: test
  script:
    - npm install
    - npm run test:db
    - npm run test:server
  artifacts:
    when: always
    reports:
        junit: artifacts/junit-*.xml
